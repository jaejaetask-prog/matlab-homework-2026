N,M = map(int,input().split());

L = [0]*N;

for _ in range(M):
    i, j, k = map(int, input().split());
    for u in range(i,j+1):
        L[u-1] = k;

for s in range(N):
    print(L[s],end=' ')
%% 반복문 print(*L)로 대체 가능   
