N, M = map(int, input().split());

B = list(range(1,N+1));

for _ in range(M):
    i, j = map(int, input().split());
    for k in range((j-i)//2+1):
        B[i+k-1], B[j-k-1] = B[j-k-1], B[i+k-1];

print(*B)
